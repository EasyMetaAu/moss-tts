# Runtime deps for local: FastAPI wrapper + hybrid e2e (ONNX + subprocess llama-moss-tts)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
soundfile>=0.12
numpy>=1.24,<3
# CPU-only onnxruntime avoids cuDNN dependency for the audio decoder step.
# Switch to onnxruntime-gpu>=1.20 if cuDNN 9.x is available and AUDIO_DECODER_CPU=0.
onnxruntime>=1.20
tokenizers>=0.20
